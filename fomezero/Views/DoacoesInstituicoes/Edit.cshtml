@model fomezero.Models.DoacoesInstituico

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>DoacoesInstituico</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="DoacaoId" class="control-label"></label>
                <select asp-for="DoacaoId" class="form-control" asp-items="ViewBag.DoacaoId"></select>
                <span asp-validation-for="DoacaoId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="InstituicaoId" class="control-label"></label>
                <select asp-for="InstituicaoId" class="form-control" asp-items="ViewBag.InstituicaoId"></select>
                <span asp-validation-for="InstituicaoId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
