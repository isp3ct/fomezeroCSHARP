@model IEnumerable<fomezero.Models.RetiradaDoaco>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DataAgendada)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataRetirada)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Beneficiario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Doacao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LocalRetirada)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DataAgendada)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataRetirada)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Beneficiario.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Doacao.DescricaoAlimento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LocalRetirada.Endereco)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
